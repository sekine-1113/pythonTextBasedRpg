import time

print("SETUP...")
time.sleep(1.12398543048)
print("...DONE!")

config = {
    "run": "run.exe",
    "setup": "setup.exe",
    "reset": "reset.exe",
    "uninstall": "uninstall.exe",
    "cleanup": "cleanup.exe"
}

print(config)